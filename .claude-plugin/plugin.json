{
  "name": "sherpa",
  "description": "Delegates file exploration, search, and batch transformation to a local model (Ollama / llama.cpp / LM Studio), preserving the orchestrator's context.",
  "version": "0.1.0",
  "mcpServers": {
    "sherpa": {
      "command": "npx",
      "args": ["-y", "sherpa-mcp"]
    }
  }
}
